<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style TargetType="ListViewItem" x:Key="MessageListItem">
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="DarkSeaGreen" />
                <Setter Property="BorderThickness" Value="2" />
            </Trigger>
        </Style.Triggers>
        <Setter Property="Background" Value="#FF8CAFB3" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListViewItem">
                    <Border 
                        Background="{TemplateBinding Background}"
                        MinHeight="70"
                        Margin="0,0,0,1">
                        
                        <!-- Vertical stack panel for displaying Header above Body attributes of Message -->
                        <StackPanel 
                            Orientation="Vertical">

                            <Border Background="DarkGray" />
                            <StackPanel>
                                <Label Content="{Binding Sender}" Foreground="#FF0057B7" FontWeight="Bold" />
                                <Label Content="{Binding Body}" Foreground="#FF000E5A" />
                            </StackPanel>

                        </StackPanel>
                                    
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>